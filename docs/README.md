# Documentation

To build the documentation, open a terminal and run:

```
pip install -r requirements-docs.txt
make clean
make html
```

---------------------------------------------------------------------------------------------------------
